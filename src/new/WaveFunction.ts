
export default class WaveFunction {

    outputWidth: number;
    outputHeight: number;

    wavefunction: Array<number[]>;
    entropy_stack: Array<[number,number]> = [];

    constructor(outputWidth: number, outputHeight: number) {

    }

    collapseTile() { //chooses one possiblitiy based on frequency hints

    }

    //helpers
    calculateEntropy(possibilities: number[]): number {

        return null;
    }

    
    sortedInsert() {

    }

}